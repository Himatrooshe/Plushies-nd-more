export default function SmoothScroll({ children }) {
  // For now, just return children without smooth scrolling
  // This can be enhanced later with a Remix-compatible smooth scroll solution
  return <>{children}</>;
}

